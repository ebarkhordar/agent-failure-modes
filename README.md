# agent-failure-modes

A citable corpus of verified failure modes in LLM agent infrastructure,
maintained by Ehsan Barkhordar. Every entry is a real defect in a widely used
open source agent framework, reproduced in a clean Docker container against the
repo's HEAD before it was reported, then traced to its root cause and the
invariant it violates. Most entries link to an upstream fix.

The pattern that emerges: agent systems rarely fail in the model. They fail in
the plumbing around it, at message-conversion boundaries, in token accounting,
retrieval score math, text decomposition contracts, and template rendering.
Small correctness bugs in that plumbing silently corrupt agent behavior in ways
that look like "the model being dumb."

## Taxonomy

| Class | What breaks | n | Entries |
|---|---|---|---|
| Message-conversion boundaries | Content silently dropped, malformed, or carrying a field the target format forbids, when translating between message formats; a type or signal that does not survive the translation; a block read by position when the producer promises no order; a same-named field copied across the boundary raw, so a value the target format's contract forbids lands unvalidated | 12 | [001](entries/001-hermes-image-only-dropped.md), [002](entries/002-swarms-empty-text-block.md), [003](entries/003-camel-sharegpt-toolcall-args.md), [023](entries/023-camel-cohere-parallel-toolcalls.md), [030](entries/030-sagemaker-loop-variable-rebind.md), [040](entries/040-nbmodel-transient-protocol-field-persisted.md), [041](entries/041-giskard-pydantic-extra-ignore-drops-params.md), [044](entries/044-jupyter-mcp-output-type-sniffed-from-string.md), [049](entries/049-giskard-anthropic-refusal-lost-without-explanation.md), [053](entries/053-deepeval-anthropic-content-block-positional.md), [060](entries/060-jupyter-mcp-no-output-sentinel-persisted.md), [070](entries/070-camel-cohere-finish-reason-vocabulary.md) |
| Text decomposition contracts | Two line/character decompositions disagree; positions desync; a regex cleanup pass deletes text the parser would have called code; one function's two return paths normalize newlines differently; a line-number regex matches a padded token a "lossless" fold cannot round-trip | 7 | [007](entries/007-serena-splitlines-desync.md), [008](entries/008-serena-glob-question.md), [020](entries/020-serena-delete-last-line-no-newline.md), [022](entries/022-serena-read-file-unmigrated-splitter.md), [033](entries/033-hindsight-separator-strip-before-fence-detection.md), [034](entries/034-serena-fallback-decode-skips-newline-translation.md), [055](entries/055-headroom-cross-turn-dedup-zero-padded-fold.md) |
| Streaming & usage accounting | Streamed deltas and token counts wrong, doubled, or incomplete; a token that is extracted and returned but never reaches its counter; a cache-inclusive total priced as uncached; an optional wire count read as an int; output set from the visible-answer count while total keeps the billed total, so input + output != total when the model reasons; an adapter whose prompt-token total already includes cache tokens forwards no cache args to the cost call, so cache-read tokens are billed at the uncached prompt rate; one provider adapter reads only the visible-answer token field its siblings fold reasoning into, so output tokens and the derived cost undercount every thinking-model call | 9 | [005](entries/005-sambanova-stale-delta.md), [006](entries/006-anthropic-cache-tokens-lost.md), [031](entries/031-anthropic-streaming-cumulative-usage-summed.md), [035](entries/035-gemini-two-key-delta-never-reaches-elif.md), [036](entries/036-hindsight-tokens-extracted-but-never-counted.md), [054](entries/054-harness-sdk-ollama-optional-token-arithmetic.md), [065](entries/065-giskard-gemini-usage-total-mismatch.md), [072](entries/072-openlit-cache-inclusive-input-billed-at-prompt-rate.md), [074](entries/074-deepeval-gemini-thoughts-token-cost-dropped.md) |
| Indexing, ordering & counting contracts | Producer and consumers disagree on the index base or the ordering; a fixed index reads the wrong end; an enumeration yields an order-dependent subset of the pairs it owes; a compacted list is zipped back onto the original; an exclusive end index is consumed as if it pointed at content; a stale index outlives a length-changing deletion; an accumulator appends onto a leaked loop variable, dropping the members it owed; a version gate compares release strings lexicographically, so 0.10.0 sorts below 0.2.1 and a valid newer release is rejected; a buffer sized in aligned units but walked in unaligned ones, so each span overlaps its predecessor's padding | 9 | [016](entries/016-changedetection-browser-step-offbyone.md), [029](entries/029-changedetection-previous-price-reversed-index.md), [032](entries/032-hindsight-cooccurrence-swap-rebinds-outer-iterate.md), [037](entries/037-hindsight-zip-compacted-list-misattributes-memories.md), [043](entries/043-serena-search-exclusive-end-line.md), [050](entries/050-inspect-ai-compaction-stale-server-index-after-delete.md), [058](entries/058-datasets-horizontal-concat-arrow-wrong-accumulator.md), [063](entries/063-deepeval-ragas-version-lexicographic-compare.md), [071](entries/071-deepspeed-zero3-allreduce-fetch-stride-unaligned.md) |
| Template & token rendering | Tokens valid in one context crash in another, raw templates shown as rendered values, or template syntax mis-parsed by a naive splitter | 3 | [009](entries/009-changedetection-restock-token.md), [015](entries/015-changedetection-jinja-url-raw.md), [025](entries/025-changedetection-import-jinja-split.md) |
| Initialization & control flow | Variables read where never bound; guard branches left unreachable; a resource referenced before it is created, or built above a branch and consumed by only one arm; a guard that queries state the action it just issued has not settled; a bound added to one branch of a fix but never carried to the sibling branches under the same precondition | 5 | [011](entries/011-cohere-unbound-documents.md), [017](entries/017-gpt-researcher-optional-str-sentinel.md), [038](entries/038-jupyter-mcp-create-notebook-after-session.md), [042](entries/042-jupyter-mcp-stream-timeout-guard-dead.md), [059](entries/059-deepspeed-topkgating-capacity-unclamped-drop-branch.md) |
| Cache-key & hashing collisions | Distinct inputs serialize to one key; caches return the wrong entry, or dedup discards a distinct record | 2 | [013](entries/013-lightrag-argshash-collision.md), [024](entries/024-ldr-benchmark-hash-omits-identity.md) |
| Concurrency & context propagation | Work offloaded to a thread pool loses the request's ambient context; results silently drop; assembling a child context fires a teardown that consumes the parent's resource | 3 | [018](entries/018-ldr-flask-context-parallel-search.md), [021](entries/021-ldr-progressive-explorer-context.md), [056](entries/056-ldr-thread-context-teardown-mutates-parent-session.md) |
| Sync/async boundaries | A call site hard-codes one side of a may-be-async contract (awaits an already-sync value, or offloads a coroutine function to a thread), so the other side (often the dependency's default) silently returns an un-awaited coroutine instead of a result | 1 | [057](entries/057-jupyter-mcp-local-backend-to-thread-coroutine.md) |
| Tool side-effect scope | An operation's real effect exceeds its name: a tool reaches past what its caller asked for, or a reorder destroys the state it was only meant to move | 2 | [027](entries/027-jupyter-mcp-use-notebook-ui-focus-steal.md), [039](entries/039-jupyter-mcp-move-cell-rebuilds-from-source.md) |
| Retrieval & scoring math | Similarity scores misinterpreted, rankings inverted | 1 | [004](entries/004-ldr-faiss-metric-inversion.md) |
| Path & name handling | Names escape their intended directory | 1 | [010](entries/010-serena-memory-path-escape.md) |
| Resource liveness & reconnection | A cached handle to a cullable resource goes stale; the recovery path fails to recover, or rebuilds the whole record through a defaulting constructor and silently resets the fields it had no business touching | 2 | [012](entries/012-jupyter-mcp-kernel-liveness.md), [061](entries/061-jupyter-mcp-ensure-kernel-alive-binding-clobber.md) |
| Schema traversal completeness | A recursive walker skips a valid branch of the tree | 1 | [014](entries/014-pydantic-ai-schema-composition-recursion.md) |
| Error handling & success reporting | A swallowed failure is reported as success; a status flag gates the recovery path; a retry path crashes on a spec-legal input instead of recovering; a filter crashes on the very ragged-but-empty rows it exists to skip | 3 | [019](entries/019-ldr-indexed-without-embeddings.md), [051](entries/051-mcp-context-forge-retry-after-http-date-crash.md), [068](entries/068-inspect-ai-csv-ragged-row-strip-crash.md) |
| Concurrency & atomic claims | A read of shared state is mistaken for a claim on it; concurrent workers act on the same rows; a membership check and a separate subscript race a concurrent delete, so the read raises instead of degrading to empty | 3 | [026](entries/026-ldr-download-queue-no-atomic-claim.md), [045](entries/045-ldr-download-queue-claim-check-then-act.md), [067](entries/067-ldr-active-runs-toctou-check-then-subscript.md) |
| Configuration wiring & documented contracts | A documented setting reaches no code that reads it; a sentinel's documented meaning is never implemented; one of a family of sibling tools skips the shared resolution so a config ceiling does not bind there; a batch is sized by a fixed input count instead of the endpoint's token budget, so its context ceiling never bounds the request | 4 | [028](entries/028-jupyter-mcp-execution-timeout-unwired.md), [048](entries/048-jupyter-mcp-execute-code-kernel-id-discarded.md), [052](entries/052-jupyter-mcp-execute-code-timeout-bypass.md), [066](entries/066-ldr-embeddings-unbounded-batch-exceeds-context.md) |
| Observability & stale documentation | A branch discards a record with no log line, so a future regression is invisible; a correctness fix leaves behind a comment that now describes the wrong invariant | 1 | [046](entries/046-ldr-benchmark-duplicate-skip-logging.md) |
| Round-trip & export fidelity | One member of a family of serialization or export paths omits a decode or a field its siblings apply, so that path silently hands back the wrong type or drops data a save/load cycle should preserve; or a save path and its paired load path pick different codecs, so the round trip crashes on the values where the two codecs disagree | 3 | [062](entries/062-datasets-to-pandas-json-not-decoded.md), [064](entries/064-deepeval-jsonl-single-turn-drops-fields.md), [073](entries/073-deepeval-csv-metadata-codec-mismatch.md) |

## Entry format

Each entry records: the repo and surface, the root cause at file level, the
**invariant violated** (the general rule, which is the reusable part), the
trigger conditions, a minimal reproduction, and the fix.

## Method

Every claim here was executed: reproduced in a clean container against the
then-current HEAD of the target repo, with the repro included in the entry or
the linked PR. Nothing is speculative; where a root cause is inferred rather
than stepped through, the entry says so. Maintained with AI assistance; every
entry is verified before it lands.

## License

Text is CC BY 4.0. Code snippets in entries are under the license of the repo
they came from, linked per entry.
